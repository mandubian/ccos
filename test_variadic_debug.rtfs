; Test variadic parameter parsing
(defn sum-all [& numbers]
  (println "numbers type:" (type numbers))
  (println "numbers value:" numbers)
  (reduce + numbers))

; Call the function
(sum-all 1 2 3 4 5)
