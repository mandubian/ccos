- Do NOT call host capabilities `(call ...)`.
- Do NOT print or log in the implementation.
- Do NOT return strings describing the result; return structured RTFS data (map/vector).
- Avoid brittle indexing (no `(nth items 0)` without checks).
- Avoid assuming specific label keys; fall back gracefully.







