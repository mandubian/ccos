// Compatibility shim for runtime::capabilities::registry
// Re-export the CCOS CapabilityRegistry for backward compatibility during migration

pub use crate::ccos::capabilities::registry::CapabilityRegistry;
