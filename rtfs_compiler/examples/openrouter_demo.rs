// Minimal, conflict-free OpenRouter demo example used for CI/build
// Keeps dependencies light so the example compiles in CI without external keys.

fn main() {
    println!("openrouter_demo: minimal example - no runtime behavior");
}
