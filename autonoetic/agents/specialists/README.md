# Specialist Agent Group

This directory contains specialist "hand" agents used by `planner.default`.

Current bundles:

- `researcher.default`
- `architect.default`
- `coder.default`
- `debugger.default`
- `evaluator.default`
- `auditor.default`
