fn main() {
    // This should fail if calculate_match_score is not in scope or defined
    // We try to call it as if it were available in bin context, but we can't easily test bin context from outside.
}
